import { Text, View } from 'react-native';

export default function Chat() {
  return (
    <View className='flex-1 items-center justify-center bg-background'>
      <Text className='text-h3 font-poppins-semibold text-text-primary'>Чат</Text>
    </View>
  );
}
